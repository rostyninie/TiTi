$("ul.errors").html("")
<% if @coordonne_bancaire.errors.any? %>
  <% @coordonne_bancaire.errors.full_messages.each do |message| %>
$("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
<% end%>
<% else %>
$(".coordonne-index").html("<%= escape_javascript(render(:partial=>'index')) %>")
$("#coordonne-modal").modal("hide")
$.jGrowl("<%= flash[:notice] %>", {header: ''})
<% end %>
