<%= form_for(@categori_salaire) do |f| %>
  <% if @categori_salaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categori_salaire.errors.count, "error") %> prohibited this categori_salaire from being saved:</h2>

      <ul>
      <% @categori_salaire.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :is_active %><br>
    <%= f.check_box :is_active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
