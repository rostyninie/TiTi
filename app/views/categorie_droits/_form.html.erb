<%= form_for(@categorie_droit) do |f| %>
  <% if @categorie_droit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorie_droit.errors.count, "error") %> prohibited this categorie_droit from being saved:</h2>

      <ul>
      <% @categorie_droit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :categorie_id %><br>
    <%= f.number_field :categorie_id %>
  </div>
  <div class="field">
    <%= f.label :droit_id %><br>
    <%= f.number_field :droit_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
